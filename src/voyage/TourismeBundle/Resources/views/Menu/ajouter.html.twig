{% extends "::base.html.twig" %}

{% block title %}voyageTourismeBundle:Menu:ajouter{% endblock %}

{% block body %}
    <h1>Welcome to the Menu:ajouter page</h1>
    <div class="well">
        <form method="post" {% if edit is not defined %} action="{{path('menu_ajouter')}}" {% else%} action="{{path('menu_ajouter',{'id':edit})}}" {% endif%} {{ form_enctype(form) }}>
            {{ form_widget(form) }}
            <input type="submit" class="btn btn-primary" {% if edit is not defined %} value="Ajouter" {% else%} value="Modifier" {% endif%}/>
        </form>
    </div>
{% endblock %}
